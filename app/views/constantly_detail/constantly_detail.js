'use strict';
angular.module('console.constantly_detail', [
        {
            files: [

            ]
        }
    ])
    .controller('constDetailCtrl', ['$state', '$http', '$scope', '$rootScope',
        function($state, $http, $scope, $rootScope){

        }]);