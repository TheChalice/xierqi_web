'use strict';
angular.module('console.secret_detail', [
        {
            files: [

            ]
        }
    ])
    .controller('secretDetailCtrl', ['$state', '$http', '$scope', '$rootScope',
        function($state, $http, $scope, $rootScope){

        }]);